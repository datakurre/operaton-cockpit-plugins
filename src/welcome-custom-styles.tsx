/**
 * Welcome Custom Styles Plugin
 *
 * This plugin applies custom stylesheets to the Welcome UI without any
 * JavaScript functionality. Useful for branding, theming, or UI adjustments.
 */

import './cockpit-custom-styles.scss';

/**
 * Custom styles plugin that injects a stylesheet into Welcome.
 * No render function needed - the SCSS import handles everything.
 */
export default [];
